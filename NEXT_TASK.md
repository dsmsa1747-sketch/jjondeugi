# NEXT_TASK — 다음 1개 작업만

> 한 번에 하나. 이게 끝나야 다음으로 갱신. 곁가지 금지. (Control Kit RULES)

## 작업
**오너 추천링크 실작동 (`?ref=오너코드`)**

## 무엇을
구매자가 오너의 추천링크(`...?ref=SY4F2A`)로 들어와 구매하면, 그 주문의 `ownerCode`에 해당 오너코드가 자동으로 기록되고 → 그 오너의 정산 대시보드 GMV·수익(3%)에 실제로 반영된다.

## 어디를 고치나
- `index.html`
  - 부팅 시 `location.search`의 `ref` 파라미터를 읽어 `S.refCode`에 저장(없으면 무시)
  - `checkout()`에서 주문 객체 `ownerCode`에 `S.refCode` 기록
  - 쇼핑몰 상단/배너에 "○○ 오너 추천으로 입장" 표시(있을 때만, 가짜표시 금지)

## 완료 기준 (VERIFY)
- [ ] `?ref=SY4F2A`로 접속 → 구매 → 주문의 ownerCode=SY4F2A 저장 확인
- [ ] 해당 오너(이수연) 대시보드 GMV/수익에 그 주문이 더해져 보임
- [ ] ref 없을 때 ownerCode 빈 값(가짜 적립 금지)
- [ ] 문법 OK, Math.random 0, 한국어/원/KST, 색 3개·이모지 6개 이하

## 사람(회장님)에게 물어볼 것
없음. CONTEXT/RULES로 결정 가능.

## 다음 작업 후보
- 구매자 회원/로그인 + 본인 주문내역
- 토스 실결제(테스트키)
