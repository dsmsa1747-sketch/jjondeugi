# SoccerMom 오픈 체크리스트 — "형님이 직접 해야만 하는 것"

> 제가 코드·앱·인프라 설정은 전부 만들어 드립니다.
> 하지만 아래는 **법적으로/계약상 형님 본인(사업자)만 할 수 있는 일**입니다.
> 제가 대신 못 합니다(형님 신분·카드·사업자등록이 필요). 이걸 빠뜨리면 오픈해도 사고가 납니다.
> "절대 오류 없이"는 코드에선 제가 책임지고, 아래 항목은 형님이 채워야 완성됩니다.

---

## 🔴 0. 가장 중요 — 미성년자(유소년) 영상은 법적으로 민감합니다 (꼭 읽기)
유소년 선수 영상·분석은 **개인정보보호법상 '아동의 개인정보'** 에 해당합니다. 그냥 오픈하면 안 됩니다.
- **보호자(법정대리인) 동의**를 반드시 받아야 합니다 (만 14세 미만은 법정대리인 동의 필수).
- 영상에 **다른 아이들(상대팀·관중)** 이 찍히면 그들의 개인정보도 포함됩니다 → 동의/모자이크/내부용 한정 등 처리 필요.
- 보관 기간·파기 절차를 명시해야 합니다.
- → 제가 `/privacy`(개인정보처리방침)·`/terms`(이용약관) **초안을 앱에 넣어두지만**, 실제 사업자명·연락처를 채우고 **가능하면 전문가(노무/법무) 1회 검토**를 권합니다. 이건 환불보다 더 큰 리스크라 정직하게 말씀드립니다.

## 🔴 1. 사업자 등록 (결제의 전제)
- 토스페이먼츠 정식 계약은 **사업자등록증**이 있어야 합니다 (개인사업자 가능).
- 정산받을 **사업자 명의 계좌** 필요.
- 없으면: 테스트 결제(이미 코드에 적용됨)로 시연·검증은 가능하지만 **실제 돈 수취는 불가**.

## 🔴 2. 토스페이먼츠 가맹점 신청
- https://toss.im/payments → 가맹점 신청 → 심사(보통 며칠).
- 승인되면 **실키(live key)** 발급 → 앱 `.env`의 `TOSS_SECRET_KEY` / `NEXT_PUBLIC_TOSS_CLIENT_KEY` 교체.
- (현재는 형님이 주신 테스트 키로 결제 흐름이 동작합니다.)

## 🟡 3. 로그인(OAuth) 앱 등록 — 각 콘솔에서 5~10분
- **Google**: console.cloud.google.com → OAuth 동의화면 + 클라이언트 ID → `GOOGLE_CLIENT_ID/SECRET`
- **카카오**: developers.kakao.com → 앱 생성 → REST 키 → `AUTH_KAKAO_ID`
- **네이버**: developers.naver.com → 앱 등록 → `NAVER_CLIENT_ID/SECRET`
- 각 콘솔에 **리디렉트 URL**(`https://도메인/api/auth/callback/...`) 등록 필요.

## 🟡 4. Google Cloud 배포 (이미 결제 등록됨 ✅ — 거의 형님 차례)
- 정밀분석 워커: `soccer-analysis-worker/deploy/deploy.sh` 실행 (Cloud Shell).
- 앱: `soccermom-app/` 를 Cloud Run 또는 Firebase Hosting 에 배포 (README 안내).
- Firestore 활성화 1회: `gcloud firestore databases create --location=nam5`
- Cloud Tasks 큐 + 워커 호출 서비스계정 (soccermom-integration/README의 GCP 1회 설정).

## 🟡 5. YOLO 모델(best.pt) 준비 — 정밀분석 정확도의 핵심
- Roboflow 무료 축구 데이터셋 + **Google Colab 무료 GPU** 로 1회 학습 (전부 Google 안에서 가능).
- 학습한 `best.pt` → `gs://버킷/models/best.pt` 업로드 → 워커 `MODEL_PATH` 지정.
- (없으면 폴백 모델로 동작은 하나 정확도 낮음 → 유료 판매 전 학습 권장.)
- → 원하시면 제가 Colab 학습 노트북까지 만들어 드립니다.

## 🟢 6. 도메인 (선택, 신뢰도)
- Google Domains/가비아 등에서 도메인 구입 → Cloud Run/Firebase에 연결.
- 없어도 Cloud Run 기본 URL로 오픈 가능.

## 🟢 7. 앱 스토어 (선택 — 지금은 PWA로 충분)
- 제가 만드는 앱은 **PWA(설치형 웹앱)** 라 스토어 심사 없이 "홈 화면에 추가"로 앱처럼 깔립니다 → **즉시 오픈 가능**.
- 정식 스토어 출시(App Store/Play)는 개발자 계정(애플 연 $99, 구글 $25) + 심사가 필요 → 나중에 선택.

---

## 제가 책임지는 것 (코드/앱 — "오류 없이")
- ✅ 정밀분석 워커 (Cloud Run+GPU) — 빌드/문법 검증 완료
- ✅ 웹 연결 코드 (GCS·Firestore·Cloud Tasks)
- 🔨 완성형 앱 (랜딩·분석·결제·결과·마이페이지·PWA) — `npm run build` 통과를 기준으로 제작 중
- ✅ 개인정보처리방침/이용약관 초안 (앱 내 /privacy, /terms)
- ✅ 정직 라벨·두 분석모드·연습경기 모드·팀 비교

## 오픈까지 최단 경로 (요약)
1. (나) 앱 빌드 완료 → 배포 → 동작 확인
2. (형님) OAuth 키 3개 + GCP 배포 + best.pt 학습
3. (형님) 사업자등록 → 토스 가맹점 → 실키 교체
4. (둘이) 개인정보방침 사업자정보 채우기 + 보호자동의 문구 확정
5. 오픈 🎉 (PWA로 즉시, 스토어는 나중에)
